{
  "name": "@yologuard/sandbox",
  "version": "0.0.4",
  "description": "Sandbox manager — wraps @devcontainers/cli for YoloGuard",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yologuard/yologuard.git",
    "directory": "packages/sandbox"
  },
  "publishConfig": {
    "access": "public"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "prebuild": "rm -rf dist/features/security && mkdir -p dist/features && cp -r ../../features/security dist/features/security",
    "build": "tsc",
    "test": "vitest run",
    "lint": "biome check src/"
  },
  "dependencies": {
    "@devcontainers/cli": "^0.75.0",
    "@yologuard/shared": "workspace:*"
  }
}
